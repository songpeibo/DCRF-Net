# Configurations

Formal non-blind DCRF-Net configs used in the paper main results:

- `paviau/dcrf_net.yaml`
- `chikusei/dcrf_net.yaml`
- `houston2018/dcrf_net.yaml`

Override dataset location at runtime with `--data-root` (training) or `--data-root` (evaluation).
